<h1><%= @retailer.name %></h1>

<p>Year established: <%= @retailer.year_established %></p>

<ul>
    <% @retailer.snacks.each do |snack| %>
        <li><%= link_to snack.name, snack_path(snack) %></li>
    <% end %>
</ul>

<%= link_to "Retailer Index Page", retailers_path %>

<h4>Delete by form</h4>
<%= form_tag @retailer, method: "DELETE" do %>
    <%= submit_tag "DESTROY THIS RETAILER" %>
<% end %>

<h4>Delete by link</h4>
<%= link_to "Delete this retailer by a link",
    retailer_path(@retailer),
    {method: :delete, class: "looks_like_a_button"} %>