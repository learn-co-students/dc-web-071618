<h1>All My Books</h1>

    <% @books.each do |book| %>
        <div><a href="/books/<%= book.id %>"><%= book.title %></a> by <%= book.author %></div>
    <% end %> 


<a href="/books/new">Create a new book</a>